// js/ui.js
import { formatMoney } from './helpers.js';


export function renderCapacityButtons(capacities, computeStatusFn) {
// computeStatusFn(capacity) should return a string for the meta label
}


// Recreate renderPegTable, renderModifierTable, renderSalesTable using DOM operations
